on:
  workflow_dispatch:

name: Release All Platforms

jobs:
  release-riscv64:
    name: Trigger qemu-riscv64-virt Release
    uses: release_qemu-riscv64-virt.yaml

  release-aarch64:
    name: Trigger qemu-aarch64-virt Release
    uses: release_qemu-aarch64-virt.yaml

